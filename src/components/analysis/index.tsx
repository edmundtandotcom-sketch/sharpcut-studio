import { ScanSearch } from 'lucide-react';
import { PlaceholderCard } from '../shared/PlaceholderCard';

export function AnalysisScreen() {
  return (
    <PlaceholderCard
      icon={ScanSearch}
      title="Building your sharp first cut"
      description="Transcription, silence and filler detection land in Phase P2."
    />
  );
}
