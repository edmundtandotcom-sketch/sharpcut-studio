import { Wand2 } from 'lucide-react';
import { PlaceholderCard } from '../shared/PlaceholderCard';

export function StudioScreen() {
  return (
    <PlaceholderCard
      icon={Wand2}
      title="Export Studio"
      description="Captions, crop, speed, transitions and zoom controls land in Phase P4."
    />
  );
}
