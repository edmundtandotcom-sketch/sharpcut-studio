import { ListChecks } from 'lucide-react';
import { PlaceholderCard } from '../shared/PlaceholderCard';

export function ReviewScreen() {
  return (
    <PlaceholderCard
      icon={ListChecks}
      title="Review edits"
      description="The manual IN/OUT editor and skip-preview playback land in Phase P3."
    />
  );
}
