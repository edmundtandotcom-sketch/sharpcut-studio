import { UploadCloud } from 'lucide-react';
import { PlaceholderCard } from '../shared/PlaceholderCard';

export function UploadScreen() {
  return (
    <PlaceholderCard
      icon={UploadCloud}
      title="Upload"
      description="Drag-and-drop upload, pacing presets and validation land in Phase P2."
    />
  );
}
